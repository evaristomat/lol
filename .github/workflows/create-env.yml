name: Create .env file

on:
  workflow_dispatch:

jobs:
  create-env:
    runs-on: ubuntu-latest
    steps:
      - name: Create and display .env
        run: |
          echo "BET365_API_TOKEN=${{ secrets.BETSAPI_API_KEY }}"
          echo "TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}"
          echo "TELEGRAM_CHAT_ID=${{ secrets.TELEGRAM_CHAT_ID }}"
          echo ""
          echo "=== COPIE O CONTEÚDO ABAIXO PARA SEU .env ==="
          echo ""
          echo "BET365_API_TOKEN=${{ secrets.BETSAPI_API_KEY }}"
          echo "TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}"
          echo "TELEGRAM_CHAT_ID=${{ secrets.TELEGRAM_CHAT_ID }}"